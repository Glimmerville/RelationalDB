@model IEnumerable<NewMVCproject53.Models.ZipsToCity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Citycode.CityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zippitycode.ZipCodeValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Citycode.CityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zippitycode.ZipCodeValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ZipsToCityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ZipsToCityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ZipsToCityId })
        </td>
    </tr>
}

</table>
